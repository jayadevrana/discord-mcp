# Copy to .env and fill in. Never commit the real .env.

# Required: bot token from https://discord.com/developers/applications -> your app -> Bot
DISCORD_BOT_TOKEN=

# Optional: default server (guild) id, so tools don't need guild_id every call.
# Enable Developer Mode in Discord, right-click your server -> Copy Server ID.
DISCORD_GUILD_ID=
